import React from 'react';

import { Message } from 'src/components';

import {
  ExampleSection,
  ComponentExample
} from 'docs/app/components';

const ModalExamples = () => (
  <ExampleSection title={'Types'}>
    <ComponentExample
      title={'Modal'}
      description={'A standard modal.'}
      examplePath={'containers/Modal/Types/ModalExampleModal'}
    />
    <ComponentExample
      title={'Basic'}
      description={'A modal can reduce its complexity.'}
      examplePath={'containers/Modal/Types/ModalExampleBasic'}
    />
    <ComponentExample
      title={'Scrolling Modal'}
      description={[
        'When your modal content exceeds the height of the browser the scrollable area will automatically',
        'expand to include just enough space for scrolling, without scrolling the page below.',
      ].join(' ')}
      examplePath={'containers/Modal/Types/ModalExampleScrolling'}
    >
      <Message warning>
        <code>&lt;Modal.Content image /&gt;</code> requires an image
        with wrapped markup: <code>&lt;Image wrapped /&gt; </code>
      </Message>
    </ComponentExample>
    <ComponentExample
      title={'Multiple Modals'}
      description={'Multiple modals can be displayed on top of one another.'}
      examplePath={'containers/Modal/Types/ModalExampleMultiple'}
    />
    <ComponentExample
      title={'Controlled'}
      description={'A modal can be a controlled component'}
      examplePath={'containers/Modal/Types/ModalExampleControlled'}
    />
  </ExampleSection>
);

export default ModalExamples;
