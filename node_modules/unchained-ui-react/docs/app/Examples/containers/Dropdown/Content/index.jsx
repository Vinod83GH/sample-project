import React from 'react';

import {
  ExampleSection,
  ComponentExample,
  ContributionPrompt,
} from 'docs/app/components';

const DropdownContentExamples = () => (
  <ExampleSection title={'Content'}>
    <ComponentExample
      title={'Header'}
      description={'A dropdown menu can contain a header.'}
      examplePath={'containers/Dropdown/Content/DropdownExampleHeader'}
    >
      <ContributionPrompt>
        Dropdown state is not fully managed when using the subcomponent API.
        The shorthand props API fully manages state but needs to be extended to support the markup shown here.
      </ContributionPrompt>
    </ComponentExample>
    <ComponentExample
      title={'Divider'}
      description={'A dropdown menu can contain dividers to separate related content.'}
      examplePath={'containers/Dropdown/Content/DropdownExampleDivider'}
    >
      <ContributionPrompt>
        Dropdown state is not fully managed when using the subcomponent API.
        The shorthand props API fully manages state but needs to be extended to support the markup shown here.
      </ContributionPrompt>
    </ComponentExample>
    <ComponentExample
      title={'Icon'}
      description={'A dropdown menu can contain an icon.'}
      examplePath={'containers/Dropdown/Content/DropdownExampleIcon'}
    >
      <ContributionPrompt>
        Dropdown state is not fully managed when using the subcomponent API.
        The shorthand props API fully manages state but needs to be extended to support the markup shown here.
      </ContributionPrompt>
    </ComponentExample>
    <ComponentExample
      title={'Description'}
      description={'A dropdown menu can contain a description.'}
      examplePath={'containers/Dropdown/Content/DropdownExampleDescription'}
    >
      <ContributionPrompt>
        Dropdown state is not fully managed when using the subcomponent API.
        The shorthand props API fully manages state but needs to be extended to support the markup shown here.
      </ContributionPrompt>
    </ComponentExample>
    <ComponentExample
      title={'Label'}
      description={'A dropdown menu can contain a label.'}
      examplePath={'containers/Dropdown/Content/DropdownExampleLabel'}
    >
      <ContributionPrompt>
        Dropdown state is not fully managed when using the subcomponent API.
        The shorthand props API fully manages state but needs to be extended to support the markup shown here.
      </ContributionPrompt>
    </ComponentExample>
    <ComponentExample
      title={'Message'}
      description={'A dropdown menu can contain a message.'}
      examplePath={'containers/Dropdown/Content/DropdownExampleMessage'}
    >
      <ContributionPrompt>
        Dropdown state is not fully managed when using the subcomponent API.
        The shorthand props API fully manages state but needs to be extended to support the markup shown here.
      </ContributionPrompt>
    </ComponentExample>
    <ComponentExample
      title={'Floated Content'}
      description={'A dropdown menu can contain floated content.'}
      examplePath={'containers/Dropdown/Content/DropdownExampleFloatedContent'}
    >
      <ContributionPrompt>
        Dropdown state is not fully managed when using the subcomponent API.
        The shorthand props API fully manages state but needs to be extended to support the markup shown here.
      </ContributionPrompt>
    </ComponentExample>
    <ComponentExample
      title={'Input'}
      description={'A dropdown menu can contain an input.'}
      examplePath={'containers/Dropdown/Content/DropdownExampleInput'}
    >
      <ContributionPrompt>
        Dropdown state is not fully managed when using the subcomponent API.
        The shorthand props API fully manages state but needs to be extended to support the markup shown here.
      </ContributionPrompt>
    </ComponentExample>
    <ComponentExample
      title={'Image'}
      description={'A dropdown menu can contain an image.'}
      examplePath={'containers/Dropdown/Content/DropdownExampleImage'}
    >
      <ContributionPrompt>
        Dropdown state is not fully managed when using the subcomponent API.
        The shorthand props API fully manages state but needs to be extended to support the markup shown here.
      </ContributionPrompt>
    </ComponentExample>
  </ExampleSection>
);

export default DropdownContentExamples;
