import React from 'react';

import { Form, TextArea } from 'unchained-ui-react';

const TextAreaExampleAutoHeight = () => (
  <Form>
    <TextArea placeholder={'Try adding multiple lines'} autoHeight />
  </Form>
);

export default TextAreaExampleAutoHeight;
