import React from 'react';
import DocumentTitle from 'react-document-title';

import pkg from 'package.json';

import {
  Container,
  Heading,
  Segment,
} from 'src/components';

import {
  Logo,
} from 'docs/app/components';

const IntroPage = () => {
  return (
    <DocumentTitle title={'Introduction | Unchained UI'}>
      <Container>
        <Segment basic textAlign={'center'}>
          <Logo centered size={'small'} />
          <Heading as={'h1'} textAlign={'center'}>
            Unchained UI
            <Heading.Subheading>
              {pkg.description}
            </Heading.Subheading>
          </Heading>
        </Segment>
      </Container>
    </DocumentTitle>
  );
};

export default IntroPage;
