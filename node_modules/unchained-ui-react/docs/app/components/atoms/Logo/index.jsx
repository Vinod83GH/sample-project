import React from 'react';

import {
  Image
} from 'src/components';

const Logo = (props) => <Image {...props} src={'/assets/images/logo.jpg'} />;

Logo.propTypes = Image.propTypes;

export default Logo;
